// This file ensures all component tests are discovered and run together
// Individual test files are imported automatically by Vitest

describe('Component Tests', () => {
  it('should have all component tests available', () => {
    // This test ensures the test suite is properly set up
    expect(true).toBe(true)
  })
}) 